<UserControl x:Class="DoaneDevTools.ToolWindows.SolutionScore.SolutionScoreWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="700"
             Background="#F5F5F5">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">

            <!-- Score banner -->
            <Border CornerRadius="8" Padding="24" Margin="0,0,0,16"
                    Background="{Binding ScoreBackground}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Solution Health Score" FontSize="14" Foreground="White" Opacity="0.8"/>
                        <TextBlock Text="{Binding Score, StringFormat='{}{0}/100'}"
                                   FontSize="56" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                        <TextBlock Text="{Binding ScoreLabel}" FontSize="16" Foreground="White" Opacity="0.9"/>
                        <TextBlock Text="{Binding TrendText}" FontSize="12" Foreground="White"
                                   Opacity="0.7" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                        <Button Content="Analyze Now" Padding="14,8" FontSize="13"
                                Background="White" Foreground="{Binding ScoreBackground}"
                                BorderThickness="0" FontWeight="SemiBold"
                                Command="{Binding AnalyzeCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Component scores -->
            <TextBlock Text="Component Scores" FontSize="13" FontWeight="SemiBold"
                       Foreground="#1F3864" Margin="0,0,0,8"/>
            <ItemsControl ItemsSource="{Binding ComponentScores}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="4" Padding="12,10" Margin="0,0,0,6"
                                BorderBrush="#E0E0E0" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Name}"
                                           FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <ProgressBar Grid.Column="1" Value="{Binding Score}" Maximum="100"
                                             Height="10" Margin="8,0"
                                             Foreground="{Binding ScoreColor}" Background="#E0E0E0"/>
                                <TextBlock Grid.Column="2" Text="{Binding Score, StringFormat='{}{0}/100'}"
                                           VerticalAlignment="Center" HorizontalAlignment="Right"
                                           Foreground="{Binding ScoreColor}" FontWeight="SemiBold"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Trend history -->
            <TextBlock Text="Score History" FontSize="13" FontWeight="SemiBold"
                       Foreground="#1F3864" Margin="0,16,0,8"
                       Visibility="{Binding HasHistory, Converter={StaticResource BoolVis}}"/>
            <ItemsControl ItemsSource="{Binding ScoreHistory}"
                          Visibility="{Binding HasHistory, Converter={StaticResource BoolVis}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="4" Padding="10,6" Margin="0,0,0,3"
                                BorderBrush="#E0E0E0" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Date, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                           Foreground="#666" FontSize="11"/>
                                <ProgressBar Grid.Column="1" Value="{Binding Score}" Maximum="100"
                                             Height="8" Margin="8,0" Foreground="#1F3864" Background="#E0E0E0"/>
                                <TextBlock Grid.Column="2" Text="{Binding Score, StringFormat='{}{0}/100'}"
                                           HorizontalAlignment="Right" FontSize="11" Foreground="#1F3864"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
