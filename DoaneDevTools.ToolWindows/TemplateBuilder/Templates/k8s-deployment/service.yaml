apiVersion: v1
kind: Service
metadata:
  name: {{ service-name }}
  namespace: prod
  labels:
    app: {{ service-name }}
spec:
  selector:
    app: {{ service-name }}
  ports:
    - name: http
      port: 80
      targetPort: {{ port }}
      protocol: TCP
  type: ClusterIP
